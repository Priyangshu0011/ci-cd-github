services:

  frontend:
    build: ./frontend
    ports:
      - "3005:3005"

  backend:
    build: ./backend
    ports:
      - "8090:8090"

  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: appdb
    ports:
      - "3306:3306"